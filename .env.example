GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# The API route also falls back to NEXT_PUBLIC_GOOGLE_SCRIPT_URL for local
# compatibility, but GOOGLE_SCRIPT_URL is preferred so the deployment URL stays
# out of the browser bundle.
